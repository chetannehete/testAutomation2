# Prompt: Step 08/11 — E2E TEST

---

## Chain Context

| Field | Value |
|-------|-------|
| **Invoked By** | `00_master_prompt.md` |
| **Step** | 08 of 11 |
| **Previous Step** | `07_..._prompt.md` |
| **Next Step** | `09_..._prompt.md` |

Once this step is complete, output: `✅ Step 08 complete — [one-line summary]` and proceed to Step 09.

---

## Purpose
Generate end-to-end tests for orderservice

## Conditional Trigger
> Project has 1 controllers with 6 endpoints

## Target Classes
OrderController

---

## Hard Rules (Mandatory)
- **[E2E-H1]** Use @SpringBootTest(webEnvironment = WebEnvironment.RANDOM_PORT).
- **[E2E-H2]** Use TestRestTemplate or WebTestClient for real HTTP calls.
- **[E2E-H3]** Test complete user flows — not isolated units.
- **[E2E-H4]** Include authentication setup if security is enabled.
- **[E2E-H5]** Test both success and failure paths end-to-end.
- **[E2E-H6]** Use TestContainers for all external dependencies.

## Soft Rules (Recommended)
- **[E2E-S1]** Define test scenarios as user stories (Given/When/Then).
- **[E2E-S2]** Test CRUD lifecycle: Create → Read → Update → Delete.
- **[E2E-S3]** Include performance assertions (response time < X ms) where relevant.

---

## Execution Steps
1. Map all endpoints into user-facing scenarios
2. Define happy-path E2E flows (CRUD lifecycle)
3. Define error scenarios (4xx, 5xx responses)
4. Set up full application context with TestContainers
5. Implement authentication if security is present
6. Execute scenarios using TestRestTemplate
7. Assert on HTTP responses and database state

---

## Prompt Template

You are a senior QA engineer. Generate comprehensive end-to-end tests for the following Spring Boot microservice.

## Project Overview
Project: orderservice
Total Classes: 8
Controllers: 1
Services: 1
Repositories: 1
Database: Yes (JPA)
Messaging: Kafka
Security: No

Endpoints:
  - POST /order → OrderController.createOrder
  - GET /order/order → OrderController.getOrder
  - GET /order/allOrders → OrderController.getAllOrders
  - PUT /order/{orderId} → OrderController.updateOrder
  - DELETE /order/{orderId} → OrderController.deleteOrder
  - PATCH /order/cancelOrder → OrderController.cancelOrder


## Requirements

### Mandatory (Hard Rules)
🔴 [E2E-H1] Use @SpringBootTest(webEnvironment = WebEnvironment.RANDOM_PORT).
🔴 [E2E-H2] Use TestRestTemplate or WebTestClient for real HTTP calls.
🔴 [E2E-H3] Test complete user flows — not isolated units.
🔴 [E2E-H4] Include authentication setup if security is enabled.
🔴 [E2E-H5] Test both success and failure paths end-to-end.
🔴 [E2E-H6] Use TestContainers for all external dependencies.

### Recommended (Soft Rules)
🟡 [E2E-S1] Define test scenarios as user stories (Given/When/Then).
🟡 [E2E-S2] Test CRUD lifecycle: Create → Read → Update → Delete.
🟡 [E2E-S3] Include performance assertions (response time < X ms) where relevant.

## Test Scenarios to Cover
1. Full CRUD lifecycle for each main resource
2. Error handling (invalid inputs, not found, unauthorized)
3. Data validation flows
4. Cross-service interactions (if applicable)

6. Message publication and consumption flows

## Output Format
- Produce a single Java test file: `orderserviceE2ETest.java`
- Use Given/When/Then comments for scenario documentation
- Include test data setup and teardown

Generate the complete E2E test file now.


---

*Generated by MD Agent Prompt Orchestrator*
