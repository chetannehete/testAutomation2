# Prompt: Step 11/11 — RUN ARGUMENTS

---

## Chain Context

| Field | Value |
|-------|-------|
| **Invoked By** | `00_master_prompt.md` |
| **Step** | 11 of 11 |
| **Previous Step** | `10_..._prompt.md` |
| **Next Step** | *(last step)* |

Once this step is complete, output: `✅ Step 11 complete — [one-line summary]` and proceed to Step 12.

---

## Purpose
Generate runtime execution arguments for orderservice

## Conditional Trigger
> Project analysis completed — run arguments always generated

## Target Classes
Project-level

---

## Hard Rules (Mandatory)
- **[RUN-H1]** Provide CLI commands using `java -jar` format.
- **[RUN-H2]** Include Spring profile activation (--spring.profiles.active=).
- **[RUN-H3]** Include essential JVM parameters (-Xms, -Xmx, -XX:+UseG1GC).
- **[RUN-H4]** List all required environment variables with descriptions.
- **[RUN-H5]** Provide separate commands for dev, staging, and production.
- **[RUN-H6]** Include Docker run command if containerization is applicable.

## Soft Rules (Recommended)
- **[RUN-S1]** Include Gradle/Maven commands for building the artifact.
- **[RUN-S2]** Add JMX monitoring flags for production.
- **[RUN-S3]** Include health check curl commands.
- **[RUN-S4]** Provide docker-compose example if multiple services are involved.

---

## Execution Steps
1. Identify required environment variables from configuration
2. Determine Spring profiles from codebase analysis
3. Calculate appropriate JVM memory settings for the service size
4. Build CLI commands for each environment
5. Create Docker run commands with environment mapping
6. Add build commands for Gradle/Maven
7. Include health check and monitoring commands

---

## Prompt Template

You are a DevOps engineer. Generate runtime execution arguments and run commands for the following Spring Boot microservice.

## Project Information
Project: orderservice
Database: Yes (JPA)
Messaging: Kafka
Security: No
Spring Profiles Detected: none

Configuration Properties Found:
  (none detected)


## Requirements

### Mandatory (Hard Rules)
🔴 [RUN-H1] Provide CLI commands using `java -jar` format.
🔴 [RUN-H2] Include Spring profile activation (--spring.profiles.active=).
🔴 [RUN-H3] Include essential JVM parameters (-Xms, -Xmx, -XX:+UseG1GC).
🔴 [RUN-H4] List all required environment variables with descriptions.
🔴 [RUN-H5] Provide separate commands for dev, staging, and production.
🔴 [RUN-H6] Include Docker run command if containerization is applicable.

### Recommended (Soft Rules)
🟡 [RUN-S1] Include Gradle/Maven commands for building the artifact.
🟡 [RUN-S2] Add JMX monitoring flags for production.
🟡 [RUN-S3] Include health check curl commands.
🟡 [RUN-S4] Provide docker-compose example if multiple services are involved.

## Required Sections

### 1. Environment Variables
Table: Variable | Description | Required | Default | Example

### 2. Spring Profiles
- dev: local development settings
- staging: pre-production settings
- prod: production settings

### 3. JVM Parameters
- Memory settings
- GC configuration
- Debug flags

### 4. CLI Run Commands
```bash
# Development
java -jar ...

# Staging
java -jar ...

# Production
java -jar ...
```

### 5. Docker
```bash
docker run ...
docker-compose up ...
```

### 6. Build Commands
```bash
# Gradle
./gradlew build

# Maven
mvn clean package
```

Generate all run argument documentation now.


---

*Generated by MD Agent Prompt Orchestrator*
