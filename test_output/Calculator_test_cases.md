# Test Cases: Calculator

**Package:** `com.example.calculator`
**Source:** `C:\Users\cheta\OneDrive\Desktop\aNTIGRAVITY\test automation\samples\Calculator.java`

**Generated by MD Agent** | Total test cases: **65**

---


## Method: `add(int a, int b)`

| # | Category | Description | Preconditions | Steps | Expected Result |
|---|----------|-------------|---------------|-------|-----------------|
| TC-001 | Happy Path | Verify add works with valid inputs | `Calculator` instance is created | Call `add(1, 1)` | Returns expected int value |
| TC-002 | Boundary | Pass zero for numeric parameter `a` | `Calculator` instance is created | Call `add(...)` with `a = 0` | Returns correct boundary result |
| TC-003 | Boundary | Pass negative value for `a` | `Calculator` instance is created | Call `add(...)` with `a = -1` | Returns correct result or rejects negative input |
| TC-004 | Boundary | Pass MAX value for `a` (int) | `Calculator` instance is created | Call `add(...)` with `a = int.MAX_VALUE` | Handles overflow / large value correctly |
| TC-005 | Boundary | Pass zero for numeric parameter `b` | `Calculator` instance is created | Call `add(...)` with `b = 0` | Returns correct boundary result |
| TC-006 | Boundary | Pass negative value for `b` | `Calculator` instance is created | Call `add(...)` with `b = -1` | Returns correct result or rejects negative input |
| TC-007 | Boundary | Pass MAX value for `b` (int) | `Calculator` instance is created | Call `add(...)` with `b = int.MAX_VALUE` | Handles overflow / large value correctly |

## Method: `subtract(int a, int b)`

| # | Category | Description | Preconditions | Steps | Expected Result |
|---|----------|-------------|---------------|-------|-----------------|
| TC-008 | Happy Path | Verify subtract works with valid inputs | `Calculator` instance is created | Call `subtract(1, 1)` | Returns expected int value |
| TC-009 | Boundary | Pass zero for numeric parameter `a` | `Calculator` instance is created | Call `subtract(...)` with `a = 0` | Returns correct boundary result |
| TC-010 | Boundary | Pass negative value for `a` | `Calculator` instance is created | Call `subtract(...)` with `a = -1` | Returns correct result or rejects negative input |
| TC-011 | Boundary | Pass MAX value for `a` (int) | `Calculator` instance is created | Call `subtract(...)` with `a = int.MAX_VALUE` | Handles overflow / large value correctly |
| TC-012 | Boundary | Pass zero for numeric parameter `b` | `Calculator` instance is created | Call `subtract(...)` with `b = 0` | Returns correct boundary result |
| TC-013 | Boundary | Pass negative value for `b` | `Calculator` instance is created | Call `subtract(...)` with `b = -1` | Returns correct result or rejects negative input |
| TC-014 | Boundary | Pass MAX value for `b` (int) | `Calculator` instance is created | Call `subtract(...)` with `b = int.MAX_VALUE` | Handles overflow / large value correctly |

## Method: `multiply(double a, double b)`

| # | Category | Description | Preconditions | Steps | Expected Result |
|---|----------|-------------|---------------|-------|-----------------|
| TC-015 | Happy Path | Verify multiply works with valid inputs | `Calculator` instance is created | Call `multiply(1.0, 1.0)` | Returns expected double value |
| TC-016 | Boundary | Pass zero for numeric parameter `a` | `Calculator` instance is created | Call `multiply(...)` with `a = 0` | Returns correct boundary result |
| TC-017 | Boundary | Pass negative value for `a` | `Calculator` instance is created | Call `multiply(...)` with `a = -1` | Returns correct result or rejects negative input |
| TC-018 | Boundary | Pass MAX value for `a` (double) | `Calculator` instance is created | Call `multiply(...)` with `a = double.MAX_VALUE` | Handles overflow / large value correctly |
| TC-019 | Boundary | Pass zero for numeric parameter `b` | `Calculator` instance is created | Call `multiply(...)` with `b = 0` | Returns correct boundary result |
| TC-020 | Boundary | Pass negative value for `b` | `Calculator` instance is created | Call `multiply(...)` with `b = -1` | Returns correct result or rejects negative input |
| TC-021 | Boundary | Pass MAX value for `b` (double) | `Calculator` instance is created | Call `multiply(...)` with `b = double.MAX_VALUE` | Handles overflow / large value correctly |

## Method: `divide(double numerator, double denominator)`

| # | Category | Description | Preconditions | Steps | Expected Result |
|---|----------|-------------|---------------|-------|-----------------|
| TC-022 | Happy Path | Verify divide works with valid inputs | `Calculator` instance is created | Call `divide(1.0, 1.0)` | Returns expected double value |
| TC-023 | Boundary | Pass zero for numeric parameter `numerator` | `Calculator` instance is created | Call `divide(...)` with `numerator = 0` | Returns correct boundary result |
| TC-024 | Boundary | Pass negative value for `numerator` | `Calculator` instance is created | Call `divide(...)` with `numerator = -1` | Returns correct result or rejects negative input |
| TC-025 | Boundary | Pass MAX value for `numerator` (double) | `Calculator` instance is created | Call `divide(...)` with `numerator = double.MAX_VALUE` | Handles overflow / large value correctly |
| TC-026 | Boundary | Pass zero for numeric parameter `denominator` | `Calculator` instance is created | Call `divide(...)` with `denominator = 0` | Returns correct boundary result |
| TC-027 | Boundary | Pass negative value for `denominator` | `Calculator` instance is created | Call `divide(...)` with `denominator = -1` | Returns correct result or rejects negative input |
| TC-028 | Boundary | Pass MAX value for `denominator` (double) | `Calculator` instance is created | Call `divide(...)` with `denominator = double.MAX_VALUE` | Handles overflow / large value correctly |
| TC-029 | Exception | Trigger ArithmeticException from `divide` | `Calculator` instance is created | Call `divide(...)` with inputs that cause `ArithmeticException` | `ArithmeticException` is thrown with a meaningful message |

## Method: `factorial(int n)`

| # | Category | Description | Preconditions | Steps | Expected Result |
|---|----------|-------------|---------------|-------|-----------------|
| TC-030 | Happy Path | Verify factorial works with valid inputs | `Calculator` instance is created | Call `factorial(1)` | Returns expected long value |
| TC-031 | Boundary | Pass zero for numeric parameter `n` | `Calculator` instance is created | Call `factorial(...)` with `n = 0` | Returns correct boundary result |
| TC-032 | Boundary | Pass negative value for `n` | `Calculator` instance is created | Call `factorial(...)` with `n = -1` | Returns correct result or rejects negative input |
| TC-033 | Boundary | Pass MAX value for `n` (int) | `Calculator` instance is created | Call `factorial(...)` with `n = int.MAX_VALUE` | Handles overflow / large value correctly |
| TC-034 | Exception | Trigger IllegalArgumentException from `factorial` | `Calculator` instance is created | Call `factorial(...)` with inputs that cause `IllegalArgumentException` | `IllegalArgumentException` is thrown with a meaningful message |

## Method: `isEven(int number)`

| # | Category | Description | Preconditions | Steps | Expected Result |
|---|----------|-------------|---------------|-------|-----------------|
| TC-035 | Happy Path | Verify isEven works with valid inputs | `Calculator` instance is created | Call `isEven(1)` | Returns expected boolean value |
| TC-036 | Boundary | Pass zero for numeric parameter `number` | `Calculator` instance is created | Call `isEven(...)` with `number = 0` | Returns correct boundary result |
| TC-037 | Boundary | Pass negative value for `number` | `Calculator` instance is created | Call `isEven(...)` with `number = -1` | Returns correct result or rejects negative input |
| TC-038 | Boundary | Pass MAX value for `number` (int) | `Calculator` instance is created | Call `isEven(...)` with `number = int.MAX_VALUE` | Handles overflow / large value correctly |
| TC-039 | Happy Path | Verify `isEven` returns true | `Calculator` instance is created | Call `isEven(...)` with inputs expected to yield `true` | Returns `true` |
| TC-040 | Happy Path | Verify `isEven` returns false | `Calculator` instance is created | Call `isEven(...)` with inputs expected to yield `false` | Returns `false` |

## Method: `sumAll(List<Double> numbers)`

| # | Category | Description | Preconditions | Steps | Expected Result |
|---|----------|-------------|---------------|-------|-----------------|
| TC-041 | Happy Path | Verify sumAll works with valid inputs | `Calculator` instance is created | Call `sumAll(mockList)` | Returns expected double value |
| TC-042 | Edge Case | Pass null for parameter `numbers` (List<Double>) | `Calculator` instance is created | Call `sumAll(...)` with `numbers = null` | Throws NullPointerException or handles gracefully |
| TC-043 | Edge Case | Pass empty collection/array for `numbers` | `Calculator` instance is created | Call `sumAll(...)` with `numbers` as empty | Handles empty input gracefully |

## Method: `storeInMemory(double value)`

| # | Category | Description | Preconditions | Steps | Expected Result |
|---|----------|-------------|---------------|-------|-----------------|
| TC-044 | Happy Path | Verify storeInMemory works with valid inputs | `Calculator` instance is created | Call `storeInMemory(1.0)` | Method executes without error |
| TC-045 | Boundary | Pass zero for numeric parameter `value` | `Calculator` instance is created | Call `storeInMemory(...)` with `value = 0` | Returns correct boundary result |
| TC-046 | Boundary | Pass negative value for `value` | `Calculator` instance is created | Call `storeInMemory(...)` with `value = -1` | Returns correct result or rejects negative input |
| TC-047 | Boundary | Pass MAX value for `value` (double) | `Calculator` instance is created | Call `storeInMemory(...)` with `value = double.MAX_VALUE` | Handles overflow / large value correctly |
| TC-048 | Happy Path | Verify state change after calling `storeInMemory` | `Calculator` instance is created | Call `storeInMemory(...)` and inspect object state | Object state is updated as expected |

## Method: `recallMemory()`

| # | Category | Description | Preconditions | Steps | Expected Result |
|---|----------|-------------|---------------|-------|-----------------|
| TC-049 | Happy Path | Verify recallMemory works with valid inputs | `Calculator` instance is created | Call `recallMemory()` | Returns expected double value |

## Method: `clear()`

| # | Category | Description | Preconditions | Steps | Expected Result |
|---|----------|-------------|---------------|-------|-----------------|
| TC-050 | Happy Path | Verify clear works with valid inputs | `Calculator` instance is created | Call `clear()` | Method executes without error |
| TC-051 | Happy Path | Verify state change after calling `clear` | `Calculator` instance is created | Call `clear(...)` and inspect object state | Object state is updated as expected |

## Method: `getHistory()`

| # | Category | Description | Preconditions | Steps | Expected Result |
|---|----------|-------------|---------------|-------|-----------------|
| TC-052 | Happy Path | Verify getHistory works with valid inputs | `Calculator` instance is created | Call `getHistory()` | Returns expected String value |

## Method: `formatNumber(double value, int decimalPlaces)`

| # | Category | Description | Preconditions | Steps | Expected Result |
|---|----------|-------------|---------------|-------|-----------------|
| TC-053 | Happy Path | Verify formatNumber works with valid inputs | `Calculator` instance is created | Call `formatNumber(1.0, 1)` | Returns expected String value |
| TC-054 | Boundary | Pass zero for numeric parameter `value` | `Calculator` instance is created | Call `formatNumber(...)` with `value = 0` | Returns correct boundary result |
| TC-055 | Boundary | Pass negative value for `value` | `Calculator` instance is created | Call `formatNumber(...)` with `value = -1` | Returns correct result or rejects negative input |
| TC-056 | Boundary | Pass MAX value for `value` (double) | `Calculator` instance is created | Call `formatNumber(...)` with `value = double.MAX_VALUE` | Handles overflow / large value correctly |
| TC-057 | Boundary | Pass zero for numeric parameter `decimalPlaces` | `Calculator` instance is created | Call `formatNumber(...)` with `decimalPlaces = 0` | Returns correct boundary result |
| TC-058 | Boundary | Pass negative value for `decimalPlaces` | `Calculator` instance is created | Call `formatNumber(...)` with `decimalPlaces = -1` | Returns correct result or rejects negative input |
| TC-059 | Boundary | Pass MAX value for `decimalPlaces` (int) | `Calculator` instance is created | Call `formatNumber(...)` with `decimalPlaces = int.MAX_VALUE` | Handles overflow / large value correctly |
| TC-060 | Exception | Trigger IllegalArgumentException from `formatNumber` | `Calculator` instance is created | Call `formatNumber(...)` with inputs that cause `IllegalArgumentException` | `IllegalArgumentException` is thrown with a meaningful message |

## Method: `Calculator()`

| # | Category | Description | Preconditions | Steps | Expected Result |
|---|----------|-------------|---------------|-------|-----------------|
| TC-061 | Happy Path | Verify Calculator works with valid inputs | None | Call `new Calculator()` | Object is created successfully |

## Method: `Calculator(double initialValue)`

| # | Category | Description | Preconditions | Steps | Expected Result |
|---|----------|-------------|---------------|-------|-----------------|
| TC-062 | Happy Path | Verify Calculator works with valid inputs | None | Call `new Calculator(1.0)` | Object is created successfully |
| TC-063 | Boundary | Pass zero for numeric parameter `initialValue` | None | Call `Calculator(...)` with `initialValue = 0` | Returns correct boundary result |
| TC-064 | Boundary | Pass negative value for `initialValue` | None | Call `Calculator(...)` with `initialValue = -1` | Returns correct result or rejects negative input |
| TC-065 | Boundary | Pass MAX value for `initialValue` (double) | None | Call `Calculator(...)` with `initialValue = double.MAX_VALUE` | Handles overflow / large value correctly |

---

*Generated by [MD Agent](https://github.com/md-agent) — a custom test case & documentation generator for Java.*
